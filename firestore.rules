rules_version = '2';

service firebase.storage {
  match /databases/{database}/documents {
  	match /users/{targetUid} {
      allow read: if request.auth != null; // TODO
      allow write: if request.auth != null && targetUid == request.auth.uid;
    }
  }
}
